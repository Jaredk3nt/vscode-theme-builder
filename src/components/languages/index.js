import JSX, { EXISTING_SCOPES as EXISTING_SCOPES_JSX } from './jsx';

export const LANG_MAP = {
  jsx: {
    id: 'jsx',
    name: 'JSX',
    preview: JSX,
    existingScopes: EXISTING_SCOPES_JSX
  }
}